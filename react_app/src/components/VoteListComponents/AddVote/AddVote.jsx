import { useState } from "react";
import s from "./AddVote.module.css";

export const AddVote = ({elementData}) => {
    const [element, sEtelement] = useState(elementData);

    return (
        <>
            <div className={s.wrapper}>
                <form action="">
                    
                </form>
            </div>
        </>
    )
}